# Client (Vite) build-time environment variables.
# Copy to client/.env (or .env.local) and edit. Only VITE_-prefixed vars are
# exposed to the browser bundle via import.meta.env.

# Contact email shown on the public landing page ("Contact us" button).
VITE_CONTACT_EMAIL=scriptayo@gmail.com
